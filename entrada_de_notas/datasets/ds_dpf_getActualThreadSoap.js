function createDataset(fields, constraints, sortFields) { log.info("Digte Public Form - @@@ Inicio Dataset ds_dpf_getActualThreadSoap.js"); var dataset = DatasetBuilder.newDataset(); var user = ""; var password = ""; var company = ""; var processInstanceId = ""; var stateSequence = ""; var serverUrl = ""; var connection = null; var isr = null; var la = null; try { if (constraints != null) { for (var i = 0; i < constraints.length; i++) { if (constraints[i].fieldName == "user" && constraints[i].initialValue != "") { user = constraints[i].initialValue; } else if (constraints[i].fieldName == "password" && constraints[i].initialValue != "") { password = constraints[i].initialValue; } else if (constraints[i].fieldName == "company" && constraints[i].initialValue != "") { company = constraints[i].initialValue; } else if (constraints[i].fieldName == "serverUrl" && constraints[i].initialValue != "") { serverUrl = constraints[i].initialValue; } else if (constraints[i].fieldName == "processInstanceId" && constraints[i].initialValue != "") { processInstanceId = constraints[i].initialValue; } else if (constraints[i].fieldName == "stateSequence" && constraints[i].initialValue != "") { stateSequence = constraints[i].initialValue; } } var message = ""; if (user == "") { message = "@@ Parametro 'user' não informado."; } else if (password == "") { message = "@@ Parametro 'password' não informado."; } else if (company == "") { message = "@@ Parametro 'company' não informado."; } else if (serverUrl == "") { message = "@@ Parametro 'serverUrl' não informado."; } else if (processInstanceId == "") { message = "@@ Parametro 'processInstanceId' não informado."; } else if (stateSequence == "") { message = "@@ Parametro 'stateSequence' não informado."; } if (message != "") { dataset.addColumn("message"); dataset.addRow([message]); return dataset; } log.info('Digte Public Form - chamada rest: ' + serverUrl + '/webdesk/ECMWorkflowEngineService?wsdl'); var url = new java.net.URL(serverUrl + "/webdesk/ECMWorkflowEngineService?wsdl"); connection = url.openConnection(); connection.setDoOutput(true); log.info('Digte Public Form - conexao realizada'); var postData = new java.lang.StringBuilder(); postData.append('<soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/" xmlns:ws="http://ws.workflow.ecm.technology.totvs.com/">'); postData.append("<soapenv:Header/>"); postData.append("<soapenv:Body>"); postData.append("<ws:getActualThread>"); postData.append("<username>" + user + "</username>"); postData.append("<password>" + password + "</password>"); postData.append("<companyId>" + company + "</companyId>"); postData.append("<processInstanceId>" + processInstanceId + "</processInstanceId>"); postData.append("<stateSequence>" + stateSequence + "</stateSequence>"); postData.append("</ws:getActualThread>"); postData.append("</soapenv:Body>"); postData.append("</soapenv:Envelope>"); connection.setRequestMethod("POST"); var SOAPAction = "getActualThread"; connection.setRequestProperty("Content-Type", "text/xml; charset=utf-8"); connection.setRequestProperty("SOAPAction", SOAPAction); var os = connection.getOutputStream(); os.write(postData.toString().getBytes()); os.flush(); var retorno = connection.getResponseCode(); isr = new java.io.InputStreamReader(connection.getInputStream()); la = new java.io.BufferedReader(isr); var mensagem = new java.lang.String(postData); var posIni = mensagem.indexOf("<password>") + 10; var posFim = mensagem.indexOf("</password>"); var senha = mensagem.substring(posIni,posFim); mensagem = mensagem.replace("<password>" + senha + "</password>", "<password>************</password>" ); log.info('Digte Public Form - xml: >>>>>>> '+mensagem); var responseString = ""; var outputString = ""; /* Write the SOAP message response to a String. */ while ((responseString = la.readLine()) != null) { outputString = outputString + responseString; } var dbf = javax.xml.parsers.DocumentBuilderFactory.newInstance(); var db = dbf.newDocumentBuilder(); var stringReader = new java.io.StringReader(outputString); var xml = db.parse( new org.xml.sax.InputSource(stringReader)); var actualThread = xml.getElementsByTagName("ActualThread"); stringReader.close(); if (actualThread.getLength() > 0) { var actualThreadValue = actualThread.item(0).getTextContent().trim(); dataset.addColumn("actualThread"); dataset.addRow([actualThreadValue]); return dataset; } else { dataset.addColumn("message"); dataset.addRow(["@@ Falha ao consultar Thread."]); return dataset; } } else { dataset.addColumn("message"); dataset.addRow(["@@ Não foram informados os parâmetros."]); return dataset; } } catch (e) { log.info("Digte Public Form - @@ Catch exceção ao executar startProcess " + e); dataset.addColumn("message"); dataset.addRow(["@@ Catch exceção ao executar startProcess " + e]); return dataset; } finally { if(isr != null) isr.close(); if(la != null) la.close(); if(connection != null) connection.disconnect(); } log.info("Digte Public Form - @@@ Fim Dataset ds_dpf_getActualThreadSoap.js"); return dataset; }